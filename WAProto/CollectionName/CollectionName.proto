syntax = "proto3";
package proto;

/// WhatsApp Version: 2.3000.1037160418

enum CollectionName {
    COLLECTION_NAME_UNKNOWN = 0;
    REGULAR = 1;
    REGULAR_LOW = 2;
    REGULAR_HIGH = 3;
    CRITICAL_BLOCK = 4;
    CRITICAL_UNBLOCK_LOW = 5;
}
